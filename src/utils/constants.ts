export enum UserRole {
  ADMIN = "admin",
  PARTNER = "partner",
  CAPTAIN = "captain",
}

export enum OrderStatus {
  CREATED = "created",
  ASSIGNED = "assigned",
  PICKED_UP = "picked_up",
  DELIVERED = "delivered",
  CANCELLED = "cancelled",
}

